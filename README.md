# hello-world
My first Repo on Github!
Branches ... first branch ... or 'copy' (or snapshot) of a main set of code.
Commits .. this is certainly a liitle weird .. saving changes is called a 'commit' .. which is antithetical to actually saving where you often have to undo and rewrite.  
KEF
